# ═══ 路径碰撞与安全测试 ═══
# 目标：验证同标题碰撞、路径注入、特殊字符处理
# 命令：uv run chat.py --kt --script tests/e2e/scripts/collision_path.txt --report tests/e2e/results/collision_path.json --turn-timeout 90 --reset-kt-root --kt-root workspace/kt_collision_test

# T1: 摄入第一条"测试策略"知识
请记录到知识树：测试策略第一条——单元测试应覆盖所有公共接口，包括正常路径和异常路径。

# T2: 摄入同标题但不同内容 — 应生成不同 node_id，不覆盖
请记录到知识树：测试策略第二条——集成测试应验证模块间的交互契约，而非内部实现。

# T3: 第三条同标题 — 继续碰撞测试
请记录到知识树：测试策略第三条——端到端测试应模拟真实用户场景，覆盖关键业务流程。

# T4: 检索"测试策略" — 应能检索到全部三条
请检索关于测试策略的知识。

# T5: 路径注入测试 — 内容包含路径样式字符串
请记录到知识树：路径安全检查笔记——当用户输入包含 ../escape.md 或 C:/Windows/system32 等路径格式时，系统应将其视为普通文本内容而非文件路径。

# T6: 检索路径注入知识 — 验证内容被安全存储和检索
请检索关于路径安全的知识。

# T7: 特殊字符 — 表情符号和多语言
请记录到知识树：国际化测试笔记 🔔——系统需支持 中文、日本語、한국어、العربية 以及 emoji 🎉🚀✅ 等特殊字符的正确存储和检索。

# T8: 检索国际化知识
请检索关于国际化测试的知识。

# T9: 超长标题 — 测试长标题处理
请记录到知识树：这是一个用来测试系统对超长标题处理能力的知识条目，标题很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长很长——内容本身很简短：系统应对超长标题做截断或哈希处理。

# T10: 代码块内容 — 测试包含代码的摄入
请记录到知识树：代码示例知识——Python 快速排序的核心逻辑是 `def quicksort(arr): return arr if len(arr) <= 1 else quicksort([x for x in arr[1:] if x <= arr[0]]) + [arr[0]] + quicksort([x for x in arr[1:] if x > arr[0]])`。这段代码展示了列表推导和递归。

# T11: 最终检索验证
请检索关于代码示例的知识。
